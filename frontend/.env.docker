# frontend/.env.docker
# Used by Docker Compose at build time for the frontend service
# VITE_API_URL is baked into the static bundle by Vite during npm run build
# For local Docker Compose: points to backend on host port 8000
# For EC2 deployment: change this to your EC2 public IP or domain
VITE_API_URL=http://localhost:8000
